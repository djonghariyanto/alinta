import styled from "styled-components";

export const StyledPrimaryText = styled.span`
  font-size: var(--primary-text-size, inherit);
  color: var(--primary-text-color, inherit);
  font-weight: var(--primary-text-weight, inherit);
`;

export const StyledSecondaryText = styled.span`
  font-size: var(--secondary-text-size, inherit);
  color: var(--secondary-text-color, inherit);
  font-weight: var(--secondary-text-weight, inherit);
`;

